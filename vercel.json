{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && npm ci && npm run build",
  "functions": {
    "api/index.py": {
      "includeFiles": "{backend/app/**,projects/raw/**,frontend/dist/**}",
      "maxDuration": 60
    }
  }
}
